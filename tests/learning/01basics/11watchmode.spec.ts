/**
 * Watch mode flat at 
 * - Projec level
 * - Spec file level
 * - Test level , if we enable this watch mode it helps to execute the code atuomatically after saving the changes.
 * 
 *  we can enable this watchmode when you run your test in UI mode by using the command 
 * npx playwright test --ui (eye icon is watch mode )
 * after this you have two more session like playwright UI mode and trace view
 */